export type Post = {
    author: string,
    content: string,
    category: string,
    slug: string,
    title: string,
    description: string
}

export type newPost = {
    content: string,
    category: string,
    title: string
}